<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ url('article_index') }}">Gazeta internetowa</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('category_index') }}" title="{{ 'label.categories'|trans }}">
                        {{ 'label.categories'|trans }}
                    </a>
                </li>
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li>
                        <a class="nav-link" href="{{ url('comment_index') }}" title="{{ 'menu.comments'|trans }}">{{ 'menu.comments'|trans }}</a>
                    </li>
                    <li>
                        <a class="nav-link" href="{{ url('user_edit') }}" title="{{ 'menu.settings'|trans }}">{{ 'menu.settings'|trans }}</a>
                    </li>
                    <li>
                        <a class="nav-link" href="{{ url('app_logout') }}" title="{{ 'menu.logout'|trans }}">{{ 'menu.logout'|trans }}</a>
                    </li>
                {% else %}
                    <li>
                        <a class="nav-link" href="{{ url('app_login') }}" title="{{ 'menu.login'|trans }}">{{ 'menu.login'|trans }}</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>