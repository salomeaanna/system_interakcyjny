{% include 'default/_form.html.twig' with {
    page_title: 'title.article_delete'|trans({'%id%': article.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'article_index',
} %}
